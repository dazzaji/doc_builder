<%= form_for(@style) do |f| %>
  <% if @style.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@style.errors.count, "error") %> prohibited this style from being saved:</h2>

      <ul>
      <% @style.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :style_name %><br />
    <%= f.text_field :style_name %>
  </div>
  <div class="field">
    <%= f.label :style_text %><br />
    <%= f.text_area :style_text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
